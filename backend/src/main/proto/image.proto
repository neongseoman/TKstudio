syntax = "proto3";

option java_package = "org.chb.examples.lib";

service CreateImage {
  rpc sendImage ( originalImage) returns ( processedImage);
}

message originalImage {
  string name = 1;
  bytes image = 2;
}

message processedImage{
  string name = 1;
  bytes image = 2;
}